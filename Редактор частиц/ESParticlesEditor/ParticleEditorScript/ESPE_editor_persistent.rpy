init:
    if persistent.enable_hints is None:
        $ persistent.enable_hints = True
    
    if persistent.custom_music_dict is None:
        $ persistent.custom_music_dict = { }
    
    if persistent.custom_ambience_dict is None:
        $ persistent.custom_ambience_dict = { }
    